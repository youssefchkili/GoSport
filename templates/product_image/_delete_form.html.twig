<form method="post" action="{{ path('app_product_image_delete', {'id': product_image.id,'product':product.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product_image.id) }}">
    <button class="btn">Delete</button>
</form>
